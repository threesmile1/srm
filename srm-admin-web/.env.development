# 开发环境默认不要设置 VITE_API_BASE，让请求走相对路径 /api，
# 由 vite.config.ts 的 proxy 转发到本机 8080（同源、无 CORS、Cookie 正常）。
# 若设置 VITE_API_BASE=http://localhost:8080，浏览器会直连 8080，易触发 CORS；后端未启动时会出现 ERR_CONNECTION_REFUSED。
# 如需直连某台机器的后端，用 .env.development.local 覆盖（并配置后端 CORS）。
# VITE_API_BASE=
