# 随 spring.profiles.active=dev 自动合并（见 application.yml）。
# Flyway 开发期 checksum：由 DevFlywayRepairConfig（先 repair 再 migrate）处理；
# 勿仅依赖 spring.flyway.repair-on-migrate（部分版本在 validate 前不会先 repair）。
spring:
  application:
    name: srm-backend
